# Azure AI Configuration (Required for Computer Use)
AZURE_API_KEY=your-azure-api-key-here
AZURE_ENDPOINT=https://your-azure-endpoint.openai.azure.com/
MODEL_DEPLOYMENT=computer-use-preview

# Browser Use Azure Configuration (Optional, 可使用不同的 Azure 實例)
# 如果不設置，將使用上面的 Computer Use 配置
BROWSER_USE_AZURE_API_KEY=your-browser-use-azure-api-key-here
BROWSER_USE_AZURE_ENDPOINT=https://your-browser-use-azure-endpoint.openai.azure.com/
BROWSER_USE_MODEL=gpt-4o

# OpenAI Configuration (Optional, for Browser Use with OpenAI models)
OPENAI_API_KEY=your-openai-api-key-here

# Browser Use API Configuration (Optional, for browser-use cloud service)
# BROWSER_USE_API_KEY=your-browser-use-api-key-here

# Display Configuration
SCREEN_WIDTH=1920
SCREEN_HEIGHT=1080
SCREEN_DEPTH=24
INITIAL_URL=about:blank

# Docker-specific settings - do not modify
DISPLAY=:99
